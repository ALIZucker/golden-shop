import { CoSizeClickStyleDirective } from './co-size-click-style.directive';

describe('CoSizeClickStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new CoSizeClickStyleDirective();
    expect(directive).toBeTruthy();
  });
});
