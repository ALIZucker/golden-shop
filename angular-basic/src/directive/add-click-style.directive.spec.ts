import { AddClickStyleDirective } from './add-click-style.directive';

describe('AddClickStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new AddClickStyleDirective();
    expect(directive).toBeTruthy();
  });
});
